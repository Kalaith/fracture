[package]
name = "fracture-command"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"
macroquad-toolkit = { path = "../macroquad-toolkit" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1.3"
rand = "0.8"

# Networking dependencies (will be integrated in Phase 3)
# laminar = "0.5"
# tokio = { version = "1.0", features = ["rt-multi-thread", "sync"] }
